    <header id="navMenu">
      <!-- Fixed navbar -->
      <nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark">
        <!--<a class="navbar-brand" href="#"><%= image_tag('monte.png', :alt => 'image', :width => "40", :height => "35") %> MONTE</a>-->
        <% if current_user.nil? %>
          <%= link_to "#{image_tag('monte.png', :alt => 'image', :width => "35", :height => "35")} Monte? sorry...".html_safe, "#", id: "logo" %>
        <% else %>
          <%= link_to "#{image_tag('monte.png', :alt => 'image', :width => "35", :height => "35")} Montessori".html_safe, root_path, id: "logo", title: "Домой" %>
        <% end %>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarCollapse">
        <% if !current_user.nil? %>	
          <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
              <a class="nav-link" href="#">Home <span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="#">Link</a>
            </li>
            <li class="nav-item">
              <a class="nav-link disabled" href="#">Disabled</a>
            </li>
	          <li class="nav-item dropdown">
	            <a class="nav-link dropdown-toggle" href="#" id="dropdown01" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Dropdown</a>
	            <div class="dropdown-menu" aria-labelledby="dropdown01">
	              <a class="dropdown-item" href="#">Action</a>
	              <a class="dropdown-item" href="#">Another action</a>
	              <a class="dropdown-item" href="#">Something else here</a>
	            </div>
	          </li>
	          <% if current_user.superadmin_role? || current_user.supervisor_role? then %>
	              <% cc = controller.controller_name %>
	 	          <% ca = controller.action_name %>
                  <li class="nav-item hidden-xs hidden-sm <%= if cc=='users' then 'active' else '' end %>"><%= link_to 'Пользователи',users_index_path, :title => 'Пользователи', :class => "nav-link" %></li>
	              <li class="nav-item visible-xs visible-sm hidden-md hidden-lg <%= if cc=='users' then 'active' else '' end %>"><%= link_to '',users_index_path, :title => 'Пользователи', :class => "nav-link" %></li>
			  <% end %>            
          </ul>
          <!--<form class="form-inline mt-2 mt-md-0">
            <input class="form-control mr-sm-2" type="text" placeholder="Search" aria-label="Search">
            <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
          </form>-->
        <div>
	     	<%= link_to ("#{image_tag(url_for(:controller => 'welcome', :action => 'avatar_show'), :alt => 'image', :width => '35', :height => '35')}" + 
	         case when current_user.superadmin_role? then " Superadmin" when current_user.supervisor_role? then " Администратор" when current_user.user_role? then " Оператор" else " Немо" end +
	         ": #{current_user.email} Выход").html_safe, destroy_user_session_path, method: :delete, :class => "btn btn-outline-success my-2 my-sm-0" %>        
        </div>
        <% end %>        
        </div>
      </nav>
    </header>

